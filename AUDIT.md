# NEXVORA — Frontend & Design System Audit

تاريخ التدقيق: 12 أغسطس 2026

## نطاق الفحص

تم فحص تطبيق React/TypeScript، جميع المسارات، مصفوفة الصلاحيات، Zustand Store، ملفات البيانات، التصدير، وطبقتي `styles.css` و`final-polish.css`.

## ملخص تنفيذي

المنتج قوي وظيفيًا ويملك فصل أدوار فعليًا داخل الواجهة والـStore، لكنه نما تدريجيًا وأصبح نظامه البصري موزعًا بين ملفات CSS كبيرة. الاتجاه الصحيح هو توحيد الـTokens والمكونات تدريجيًا مع الحفاظ على منطق الأعمال، وليس إعادة كتابة المشروع.

## [CRITICAL]

- كانت قيم الألوان والمسافات والخطوط موزعة بين ملفي CSS، ما يسمح بانحراف بصري مستقبلي.
- لا يوجد قبل هذه الجولة مرجع رسمي يربط الشاشات بالأدوار والصلاحيات الدقيقة.
- حساب المحاسب يحتاج أن تظل صلاحية تسجيل الدفعة منفصلة عن المراجعة على مستوى الحساب، وليس الدور فقط. الحماية موجودة في الـStore والمسارات ويجب الحفاظ عليها في كل مكون جديد.
- تقرير PDF يعتمد على التنفيذ داخل المتصفح؛ أي تغيير بصري يجب ألا يكسر A4 أو RTL أو فواصل الصفحات.

## [MAJOR]

- استخدام خطين عربيين للعناوين والمتن يقلل الاتساق؛ يلزم خط عربي واحد.
- بعض الأسطح تستخدم Gradients وظلالًا أكبر من المناسب لنظام إداري مؤسسي.
- لا توجد مكتبة ملفات منفصلة للمكونات؛ المكونات العامة مركزة في `components/ui.tsx`.
- مسار لوحة التشغيل غير صريح؛ كانت الصفحة الرئيسية تتغير حسب الدور فقط. يلزم `/operations` لمدير التشغيل مع إبقاء الصفحة التنفيذية للمدير العام.
- Sidebar كثيف ويحتاج مجموعات دلالية ومسميات أكثر دقة بحسب الدور.
- بعض الأنماط تستخدم ألوان Hex مباشرة بدل Semantic Tokens.
- لا توجد Breadcrumbs موحدة للمسارات العميقة.

## [MINOR]

- بعض العناوين والملاحظات أصغر من اللازم عند التكبير.
- حالات Hover وPressed موزعة بين قواعد متعددة.
- الجداول تستجيب جيدًا، لكن تحتاج عقدًا موحدًا لتحويل الصفوف إلى بطاقات موبايل.
- أسماء الملفات القديمة الخاصة بمراحل HTML ما زالت في الجذر، لكنها خارج تطبيق React الفعلي ولم تُحذف حفاظًا على عمل المستخدم السابق.

## نقاط القوة التي يجب الحفاظ عليها

- RTL عربي فعلي وليس انعكاسًا شكليًا.
- صلاحيات Route وStore، وعزل بيانات المدرس.
- فصل لوحة CEO عن التشغيل اليومي.
- تدفقات الطلاب والمدرسين والمجموعات والحضور والمالية مكتملة محليًا.
- منع التعارضات والسعة والتكرار والاعتماد الذاتي.
- سجل تدقيق وهوية منفذ الإجراء.
- حالات تحميل وفراغ وخطأ وError Boundary.
- تصدير PDF وExcel حقيقيان.

## قرار التنفيذ

1. إنشاء طبقة Tokens مركزية وCSS مؤسسي أخير.
2. إضافة مكتبة Primitives Typed دون كسر المكونات الحالية.
3. إضافة Breadcrumbs ومسار عمليات صريح.
4. توحيد الخط والأسطح والحدود والظلال والحركة.
5. تثبيت عقود الموبايل والوصول والـDark Mode.
6. إعادة تشغيل TypeScript والبناء واختبارات الأعمال.
